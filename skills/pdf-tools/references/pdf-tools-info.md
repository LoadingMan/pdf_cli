# tools info

> **前置条件：** 先阅读 [`../pdf-shared/SKILL.md`](../../pdf-shared/SKILL.md)。

当前 CLI 未实现 `pdf-cli tools info`。

## 状态

- 该命令不在当前 Cobra 命令树中。
- `README` 和主技能文档已不再把它列为可执行命令。
- 若后续需要补齐，应先确认后端是否提供稳定且已接入的文件信息接口。

## Notes

- 不要把这个文件当作可直接执行的命令参考。
