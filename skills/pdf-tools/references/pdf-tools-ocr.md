# tools ocr

> **前置条件：** 先阅读 [`../pdf-shared/SKILL.md`](../../pdf-shared/SKILL.md)。

当前 CLI 未实现 `pdf-cli tools ocr`。

## 状态

- 该命令不在当前 Cobra 命令树中。
- 前端项目里的 OCR 更接近翻译链路参数，不是当前 CLI tools 模块的独立命令。
- `README` 和主技能文档已不再把它列为可执行命令。

## Notes

- 不要把这个文件当作可直接执行的命令参考。
